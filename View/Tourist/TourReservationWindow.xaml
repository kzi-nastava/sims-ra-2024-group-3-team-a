<Window x:Class="BookingApp.View.TourReservationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="TourReservation" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="textBoxName" Grid.Row="0"  Text="{Binding Name}" IsEnabled="False"  Grid.Column="1" Margin="5"/>

        <TextBlock Text="Location:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="textBoxLocation" Text="{Binding LocationDTOString, Mode=OneWay}" IsEnabled="False"  Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Duration:" Grid.Row="2" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="textBoxDuration" Text="{Binding Duration}" IsEnabled="False"  Grid.Row="2" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Max Tourists:" Grid.Row="3" Grid.Column="0" Margin="5"/>
        <TextBox  x:Name="textBoxMaxTourists" Text="{Binding MaxTouristNumber}" IsEnabled="False"  Grid.Row="3" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Beginning Time:" Grid.Row="4" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="textBoxBeginningTime" Text="{Binding BeginingTime}" IsEnabled="False"  Grid.Row="4" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Current Capacity:" Grid.Row="5" Grid.Column="0" Margin="5" FontWeight="Bold"/>
        <TextBox Grid.Row="5" Grid.Column="1" Margin="5" MinWidth="50"/>

        <TextBlock Text="User Name:" Grid.Row="6" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="textBoxUserName" Text="{Binding tourReservationDTO.UserId.Name}" Grid.Row="6" Grid.Column="1" Margin="5,5,500,5" MinWidth="0"/>

        <Button Content="Confirm Tour Reservation"  Grid.Row="6" Grid.Column="1" HorizontalAlignment="Right" Margin="5" Click="ConfirmReservation_Click"/>
    </Grid>
</Window>
