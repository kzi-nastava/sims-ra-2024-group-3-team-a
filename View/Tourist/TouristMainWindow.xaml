<Window x:Class="BookingApp.View.TouristMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="TourView" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="WaterMarkStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Grid Background="AntiqueWhite">
                            <Border Background="White" BorderBrush="#FF7D8683" BorderThickness="1"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0,0,0" VerticalAlignment="Center" />
                            <Label Margin="5,0,0,0" x:Name="WaterMarkLabel" Content="{TemplateBinding Tag}" VerticalAlignment="Center"
                           Visibility="Collapsed" Foreground="Gray" FontFamily="Arial"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Visibility" TargetName="WaterMarkLabel" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="DimGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="AntiqueWhite">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBox  Cursor="IBeam" Style="{StaticResource WaterMarkStyle}" Tag="Country" x:Name="searchCountryTextBox" Width="100" Margin="5"/>
            <TextBox  Cursor="IBeam" Style="{StaticResource WaterMarkStyle}" Tag="City" x:Name="searchCityTextBox" Width="100" Margin="5"/>
            <TextBox x:Name="searchDurationTextBox" Width="50" Margin="5"/>
            <ComboBox x:Name="languageComboBox" Width="100" Margin="5">
                <ComboBoxItem Content="English"/>
                <ComboBoxItem Content="Spanish"/>
                <ComboBoxItem Content="French"/>
                <ComboBoxItem Content="Serbian"/>
            </ComboBox>
            <TextBox x:Name="searchMaxTouristsTextBox" Grid.Column="0" Text="" HorizontalAlignment="Stretch" VerticalAlignment="Center" Padding="5"/>
            <Button  Background="LightPink" Content="▲" Grid.Column="1" Width="30" Click="IncreaseButton_Click" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button  Background="LightPink" Content="▼" Grid.Column="1" Width="30" Click="DecreaseButton_Click" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button  Background="LightBlue"  x:Name="buttonSearch" Content="Search" Click="Search_Click" Margin="5"/>
           
        </StackPanel>


        <DataGrid  x:Name="dataGridTour" IsReadOnly="True" ItemsSource="{Binding Tours}"  Grid.Row="1" Margin="10" AutoGenerateColumns="False">
            <DataGrid.Columns>

                <DataGridTextColumn Header="LOCATION" Width="*" Binding="{Binding LocationDTO}"/>
                <DataGridTextColumn Header="DURATION" Width="*" Binding="{Binding Duration}"/>
                <DataGridTextColumn Header="LANGUAGE" Width="*" Binding="{Binding Language}"/>
                <DataGridTextColumn Header="MAX TOURISTS" Width="*" Binding="{Binding MaxTouristNumber}"/>
                <DataGridTextColumn Header="BEGINNING TIME" Width="*" Binding="{Binding BeginingTime}"/>


            </DataGrid.Columns>
            
        </DataGrid>
        <Button Background="LightPink" x:Name="buttoneReservation" Grid.Row="2" Content="Reservation" Click="Reservation_Click" Width="80" Margin="0,0,700,0"/>
    </Grid>
</Window>
